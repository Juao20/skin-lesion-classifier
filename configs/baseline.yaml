seed: 42

data:
  root: data/
  image_size: 224
  val_split: 0.15
  test_split: 0.15
  num_workers: 4

model:
  name: resnet50
  pretrained: true
  num_classes: 7

train:
  epochs: 20
  batch_size: 32
  lr: 1.0e-4
  weight_decay: 1.0e-4
  output_dir: checkpoints/
